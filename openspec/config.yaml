# openspec/config.yaml
# Auto-generated by sdd-init on 2026-07-11
schema: spec-driven

context: |
  Tech stack: Laravel 13, FilamentPHP v5, Livewire v3+, MySQL/MariaDB 10.11.9
  Architecture: Monolithic Laravel backend with Filament admin/teacher panels + Livewire student interface
  Testing: None yet — no PHPUnit, Pest, or Dusk installed
  Style: Laravel conventions (PSR-4, Eloquent ORM), PHP 8.4, Blade/Livewire components, local storage
  Runtime: PHP 8.4.4, Composer 2.8.11, Node 22.14.0, npm 10.9.2, MariaDB 10.11.9
  GitHub CLI: Not installed — PR/issue automation requires `gh` or manual workflow

rules:
  proposal:
    - Include rollback plan for risky changes
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    guidelines:
      - Follow existing code patterns
      - Follow Laravel conventions (PSR-4, Eloquent, Service Providers, etc.)
      - Use Filament v5 best practices for panels
      - Use Livewire v3 component conventions for student views
    tdd: false
    test_command: ""
  verify:
    test_command: ""
    build_command: ""
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas

testing:
  strict_tdd: false
  reason: "No test runner installed (no PHPUnit, Pest, or Dusk). Install Laravel skeleton first, then run composer install to pull test dependencies."
  detected: "2026-07-11"
  layers:
    unit:
      available: false
      tool: null
    integration:
      available: false
      tool: null
    e2e:
      available: false
      tool: null
  coverage:
    available: false
    command: null
  quality:
    linter:
      available: false
      command: null
    type_checker:
      available: false
      command: null
    formatter:
      available: false
      command: null
